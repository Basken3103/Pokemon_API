const main = document.querySelector("#main-wrapper")
let last;

async function getPokens(limit, offset) {

    if (last) return
    let params = `?offset=${offset}&limit=${limit}`

}

// Nået til 4:36 i Steens video pokemon nr. 6 